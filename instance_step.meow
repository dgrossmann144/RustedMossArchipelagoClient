if (self.mod == "archipelago") {
  if place_meeting(self.x, self.y, oplayer) {
    global.archipelago.checkLocation(self.location)
    instance_destroy(self)
  }
  match self.type {
    case "Titania_Piece" {
      self.image_index = global.stars_
    }
  }
}