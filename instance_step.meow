if (self.mod == "archipelago") {
  if place_meeting(self.x, self.y, oplayer) {
    global.archipelago.checkLocation(self.location, self.item)
    instance_destroy(self)
  }
  match self.item.name {
    case "Titania_Piece" {
      self.image_index = global.archipelago.save.starsCache
    }
  }
}